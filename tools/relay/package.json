{
  "name": "vibepulse-numbers-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run --config vitest.config.mjs && node --test deploy.test.mjs",
    "test:merge": "node --test test.mjs",
    "build:dry": "node deploy.mjs ci-dry",
    "deploy:dry": "node deploy.mjs dry-run",
    "deploy": "node deploy.mjs deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.0.0",
    "vitest": "4.1.11",
    "wrangler": "4.125.0"
  }
}
